# App
NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_SITE_NAME=Parfum CI
NEXT_PUBLIC_WHATSAPP_NUMBER=2250000000000
NEXT_PUBLIC_INSTAGRAM_URL=
NEXT_PUBLIC_FACEBOOK_URL=
NEXT_PUBLIC_TIKTOK_URL=
NEXT_PUBLIC_CONTACT_EMAIL=contact@example.com

# Supabase public browser-safe values
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-publishable-or-anon-key

# Supabase server-only values. Never expose these with NEXT_PUBLIC_.
# SUPABASE_SECRET_KEY is the Supabase secret/service-role-style key used only by server-only code.
SUPABASE_SECRET_KEY=your-secret-or-service-role-key
SUPABASE_STORAGE_BUCKET=product-images

# Optional only for Supabase CLI local Google provider testing.
# Production Google OAuth client ID/secret are configured in the Supabase Dashboard.
SUPABASE_AUTH_EXTERNAL_GOOGLE_CLIENT_SECRET=

# Resend
RESEND_API_KEY=re_your_api_key
RESEND_FROM_EMAIL="Parfum CI <notifications@example.com>"
EMAIL_FROM="Parfum CI <notifications@example.com>"
CRON_SECRET=replace-with-a-long-random-cron-secret
NOTIFICATION_PROVIDER=development
NOTIFICATION_BATCH_SIZE=10
NOTIFICATION_MAX_ATTEMPTS=5

# Operations
ADMIN_EMAIL_ALLOWLIST=admin@example.com
AUDIT_LOG_RETENTION_DAYS=365
ORDER_TRACKING_TOKEN_SECRET=replace-with-a-long-random-secret

# Payments
PAYMENT_PROVIDER=manual
MOBILE_MONEY_INSTRUCTIONS="Orange Money / MTN / Wave payment instructions"
COD_ENABLED=true

# Playwright admin auth setup. Use only local/test staff credentials.
# Storage state is written to ignored playwright/.auth files.
PLAYWRIGHT_ADMIN_EMAIL=
PLAYWRIGHT_ADMIN_PASSWORD=
PLAYWRIGHT_OWNER_EMAIL=
PLAYWRIGHT_OWNER_PASSWORD=
PLAYWRIGHT_INVENTORY_MANAGER_EMAIL=
PLAYWRIGHT_INVENTORY_MANAGER_PASSWORD=
PLAYWRIGHT_ORDER_MANAGER_EMAIL=
PLAYWRIGHT_ORDER_MANAGER_PASSWORD=
PLAYWRIGHT_SUPPORT_EMAIL=
PLAYWRIGHT_SUPPORT_PASSWORD=
PLAYWRIGHT_INACTIVE_EMAIL=
PLAYWRIGHT_INACTIVE_PASSWORD=

# Destructive E2E is denied by default and always denied for the current production project.
# Use only local Supabase or a future explicitly allowlisted staging project.
ALLOW_DESTRUCTIVE_E2E=false
E2E_TARGET_KIND=
E2E_ALLOWED_SUPABASE_PROJECT_REF=
